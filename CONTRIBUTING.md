# Contribution Guidelines

## Setup

After cloning the repository, run the following command to ensure consistent code style:

```
git config core.hooksPath .githooks
```

This will set up the pre-commit hooks defined in the `.githooks` directory.